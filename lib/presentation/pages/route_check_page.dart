import 'package:flutter/material.dart';

class RouteCheckPage extends StatelessWidget {
  const RouteCheckPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
